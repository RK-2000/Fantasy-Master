var express = require('express');
var router = express.Router();

/*var middleware = require('./model/middleware');
var middleware1 = require('./model/middleware1');

var baseurl = require('./model/baseurl');
router.get('/baseurl',[middleware, middleware1], baseurl);

var questionLists = require('./model/questionLists');
router.get('/question/list', questionLists);

var addUserQuestion = require('./model/addUserQuestion');
router.post('/question/add', addUserQuestion);*/

module.exports = router;